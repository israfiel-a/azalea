<buildfile>

import std::build;
import std::types;

function status_code_t entry(std::build_info_t *info) = SUCCESS {
    info->outputType = FILE;

    info->inputCount = 2;
    info->inputFiles = {
        "Simple.az", "Known.az"
    };
}

